; Registry Viewer 3.8 (or later)
; Plugns list

; Filename must be in DOS format (8.3)
; Description may be no longer 70 characters

; If first character is ';' then this string is comment

PLUGTEST.RUN Plugin launcher test

; for subkeys in
; [HKEY_LOCAL_MACHINE\Software\Microsoft\Windows\CurrentVersion\Network\LanMan]
SHARPASS.RUN Shared resource password for Win9x

; for subkey [HKEY_USERS\...\RemoteAccess\Addresses]
;RASDECOD.RUN RAS parameter decoding

; for parameter "Password" of subkeys in
; [HKEY_USERS\...\SOFTWARE\Far\Plugins\FTP\Hosts]
FARDECOD.RUN FAR: FTP-plugin password decoding

; for parameter "Password" of subkeys in
; [HKEY_USERS\...\SOFTWARE\Far\Plugins\FARMail\Mailboxes]
FMDECODE.RUN FAR: Mail-plugin password decoding
